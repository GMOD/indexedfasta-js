import {
  parseStream,
  parseFile,
  parseStringSync,
  formatSync,
  formatStream,
  formatFile,
} from './api'

export default {
  parseStream,
  parseFile,
  parseStringSync,
  formatSync,
  formatStream,
  formatFile
}


